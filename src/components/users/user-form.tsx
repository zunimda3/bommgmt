export function UserForm() {
  return (
    <button
      type="button"
      style={{
        justifySelf: 'start',
        padding: '0.85rem 1.1rem',
        border: 'none',
        borderRadius: '999px',
        background: 'var(--color-accent)',
        color: '#fffaf2',
        cursor: 'pointer',
      }}
    >
      Create user
    </button>
  );
}
